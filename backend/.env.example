# Server port (Railway/Render/Fly.io will set this automatically)
PORT=3000

# Secret key for admin endpoints (regenerating bus tokens)
# Change this to a secure random string in production!
ADMIN_SECRET=change-me-to-a-secure-random-string

# SQLite database path (relative to backend root)
DB_PATH=buses.db
